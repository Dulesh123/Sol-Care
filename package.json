{
  "name": "solana-hd-wallet",
  "private": true,
  "version": "1.0.0",
  "description": "Web-based Solana HD wallet: isolated key-derivation core + React frontend, querying Alchemy over JSON-RPC.",
  "workspaces": [
    "wallet-core",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "test:core": "npm test --workspace wallet-core"
  }
}
